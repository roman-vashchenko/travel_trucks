export const selectCampers = (state) => state.campers.items;
export const selectTotalCampers = (state) => state.campers.total;
export const selectCamper = (state) => state.campers.currentItem;
export const selecteItemsSelected = (state) => state.campers.selectedItems;
export const selecteIsLoading = (state) => state.campers.isLoading;
