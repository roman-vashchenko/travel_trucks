/* eslint-disable react/prop-types */
const VehicleDetailItem = ({ name, value }) => {
  return (
    <>
      <p>{name}</p>
      <span>{value}</span>
    </>
  );
};

export default VehicleDetailItem;
